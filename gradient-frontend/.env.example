# Copy to .env.development for local npm start (optional — client.js auto-uses localhost backend)
REACT_APP_API_URL=http://127.0.0.1:8000

# Production: set in Vercel → Settings → Environment Variables (Production)
# REACT_APP_API_URL=https://your-service.onrender.com
